# DuplexChat runtime for Python 3.12.
# Keep this file aligned with pipeline/duplexchat/pyproject.toml. Install it
# in its own environment; it is intentionally incompatible with Sommelier's
# Torch/NeMo set.

# PyTorch 2.8 ABI set
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
torchcodec==0.7.0

# Audio, diarization, and dialogue separation
soundfile
pydub
librosa
demucs==4.0.1
pyannote.audio==3.3.2
speechbrain==1.0.3
diffusers==0.30.0
huggingface-hub>=0.34.0,<1.0
wrapt

# Sortformer / NeMo and Transformers Whisper LID. NeMo 2.7.3 requires
# protobuf 5.29.x, which includes google.protobuf.runtime_version required by
# current Transformers Whisper. ONNX 1.22 has a Python 3.12 wheel.
Cython==3.1.2
packaging==24.2
omegaconf
hydra-core
pytorch-lightning
transformers~=4.57.0
nemo_toolkit[asr]==2.7.3
protobuf~=5.29.5
onnx==1.22.0
onnxruntime==1.22.1

numpy
tqdm
pytest==8.4.1
