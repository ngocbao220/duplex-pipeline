# DuplexChat runtime for Python 3.12.
# Keep this file aligned with pipeline/duplexchat/pyproject.toml. Install it
# in its own environment; it is intentionally incompatible with Sommelier's
# Torch/NeMo set.

# PyTorch 2.8 ABI set
torch==2.8.0
torchaudio==2.8.0
torchvision==0.23.0
torchcodec==0.7.0

# Audio, diarization, and dialogue separation
soundfile
pydub
librosa
demucs==4.0.1
pyannote.audio==3.3.2
speechbrain==1.0.3
diffusers==0.30.0
huggingface-hub==0.33.4
wrapt

# Sortformer / NeMo. NeMo 2.3 is the runtime used by DuplexChat. Pinning a
# Python-3.12-capable ONNX release prevents pip from backtracking to ONNX 1.10
# source distributions, which cannot build their metadata on this interpreter.
Cython==3.1.2
packaging==24.2
omegaconf
hydra-core
pytorch-lightning
transformers>=4.51.0,<=4.53.3
nemo_toolkit[asr]==2.3.0
onnx==1.16.2
onnxruntime==1.22.1

numpy
tqdm
pytest==8.4.1
